import { type DrawSurface, type Scene } from './draw-surface.ts';

/**
 * STUB - signature only, no behaviour. See docs/TDD-CHARTER.md, Challenge 1.
 */
export function renderScene(_surface: DrawSurface, _scene: Scene): void {
  /* no behaviour yet */
}
